---
description: 真机报错返工必须先本地自检，禁止反复让用户开手机
globs:
alwaysApply: true
---

# 真机返工自检

L4/L5 真机报错并改代码之后：

1. **先自检代码与脚本**，证明「会画出来 / 会算对」：顶点非空、**带 index**、bbox 在正交相机内、**中心对齐 Pose 髋/头（禁止只检查 bbox 相交）**、apply 后 visible、动作前后几何有变化。能 Node 复现的不要拿真机当调试器。
2. **自检不过不得请用户开手机**。禁止用「请 Reload 再看」代替上述检查。
3. Overlay 若加载成功但网格为空或贴在画幅角落，**必须 return false 回退胶囊**，禁止藏掉胶囊后留下空白/碎三角。
