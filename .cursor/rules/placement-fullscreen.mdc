---
description: 站位框铺满预览，判定与全画面一致（FR-022）
globs: packages/render/src/placement.ts,apps/mobile/src/components/PlacementGuide.tsx
alwaysApply: false
---

# 站位 = 全画面（FR-022）

产品共识，适用于全部动作：

1. **整幅预览都可用**，**不画虚线框**。未入镜只出文案。
2. **判定与 [0,1] 画面一致**。禁止用内缩 `margin` 把贴边髋/踝判出画并冻计次/计秒。
3. **没入镜**才引导：深蹲等要髋踝；划船/推举/卧推近景只要肩肘髋（`hips_only`），脚出画不冻计数。趴地动作文案用「把手机放远」，站立可用「后退」。
4. **能算出驱动角就允许计数**；机位正面误判只出文案，不冻计数。

侧摄远侧遮挡（平板 `either_side`）与上肢近景（`hips_only`）是动作特例，不是「再画一个小框」。
