# Page override — Live training (PG-004)

Master chrome only. **Overlay rules win.**

Keep: user 2D skeleton, demo window, FR-061 joint colors, FR-090 lock, voice.

Change: surrounding chrome to OLED (end control as white or ghost pill; rep chip as dark pill + white tabular nums). Do not add MLS-style photo cards on the camera. Do not lower skeleton contrast.
